apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.security.auditLedger.configMapName }}
  namespace: {{ .Values.namespaceOverride }}
  labels:
    app.kubernetes.io/name: {{ include "ai-healing-platform.name" . }}
    app.kubernetes.io/component: backend
data: {}

