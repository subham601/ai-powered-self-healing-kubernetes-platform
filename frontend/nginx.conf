server {
  listen 8085;
  listen [::]:8085;

  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA: always serve index.html for non-file routes
  location / {
    try_files $uri $uri/ /index.html;
  }
}

